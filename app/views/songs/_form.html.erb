<%= error_messages_for(@song) %>

<table summary="Song form fields">
  <tr>
    <th><%= f.label(:category_id, "Category") %></th>
    <th><%= f.select(:category_id, Category.all.collect {|category| [ category.name, category.id ] }, { :prompt => "Category", :include_blank => true }) %></th>
  </tr>
<!-- <iframe width="560" height="315" src="song.url" frameborder="0" allowfullscreen></iframe> -->
  <tr>
    <th><%= f.label(:title) %></th>
    <td><%= f.text_field(:title) %></td>
  </tr>
  <tr>
    <th><%= f.label(:artist) %></th>
    <td><%= f.text_field(:artist) %></td>
  </tr>
  <tr>
    <th><%= f.label(:album) %></th>
    <td><%= f.text_field(:album) %></td>
  </tr>
  <tr>
    <th><%= f.label(:link) %></th>
    <td><%= f.text_field(:link) %></td>
  </tr>
  <tr>
    <th><%= f.label(:comments) %></th>
    <td><%= f.text_area(:comments) %></td>
  </tr>  
</table>
