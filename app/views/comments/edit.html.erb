<%= form_for @comment do |f| %>
	<%= f.label :title, 'Comment Title' %>
	<%= f.text_field(:title) %>
	<%= f.label :comment, 'Comment' %>
	<%= f.text_area(:comment) %>
	<%= f.submit "Boss" %>
<% end %>